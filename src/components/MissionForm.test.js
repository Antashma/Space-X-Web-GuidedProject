import React from 'react';
import {render} from '@testing-library/react';
import MissionForm from './MissionForm';

//it()  and describe()

test('MisionForm renders', () => {
    render(<MissionForm />)
})